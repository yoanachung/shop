apply plugin: "war"

bootWar {
    mainClass = "com.yon.shop.product.ProductApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
